export namespace AccessInviteLink {
  export type Args = {
    subscriberId: string
  }
}

export namespace Get {
  export type Args = {
    subscriberId: string
  }
}

export namespace GetCount {
  export type Args = {
    subscriberId: string
  }
}
